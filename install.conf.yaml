- clean: ['~']

- link:
  ~/.dotfiles-local: ''
  ~/.shell_local_after:
  ~/.shell_local_before:
  ~/.zsh_local_after:
  ~/.zsh_local_before:

- shell:
    - git submodule sync --recursive
    - git update-submodules
